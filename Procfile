web: gunicorn app:app
release: python -c "import os; import sys; import logging; logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s'); TMDB_API_KEY = os.getenv('TMDB_API_KEY'); if not TMDB_API_KEY: logging.error('TMDB_API_KEY not set in environment variables'); sys.exit(1); from generate_data import *; __main__()" 